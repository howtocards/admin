import * as React from 'react';
import styled from 'styled-components';

export const UsersPage = () => {
  return <UsersContainer>users list</UsersContainer>;
};

const UsersContainer = styled.div``;
